# Site-root open-web files are useful for a local/live site, but they do not
# belong in the WordPress.org theme package. Keep templates in docs/ instead.
/.well-known/
/about.txt
/blogroll.opml
/blogroll.txt
/colophon.txt
/feeds.txt
/humans.txt
/llms.txt
/now.txt
/robots.txt
/security.txt

# macOS Finder artifacts (package-check rejects these).
.DS_Store

# Dev-only test harness artifacts (the tests themselves are committed).
node_modules/
tests/node_modules/
tests/test-results/
tests/playwright-report/
tests/blueprint.ci.json

# Local-only working draft — keep on disk, never commit.
docs/repro/wptexturize-18549-trac-comment.md
